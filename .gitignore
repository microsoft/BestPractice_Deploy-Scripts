Feedback/

# Per-run HTML/JSON deployment reports written next to the orchestrator
Products/**/Deploy-*-Report-*.html
Products/**/Deploy-*-Report-*.json

# Ad-hoc / troubleshooting / manual scripts — not part of the shipped product.
# Folder shape is preserved via .gitkeep so contributors see the convention.
Products/**/AdHoc/*
!Products/**/AdHoc/.gitkeep
!Products/**/AdHoc/Enable-DefaultLabelForSPOLibrary.ps1

# VS Code workspace settings — personal/machine-specific
.vscode/

# Per-module Copilot "skill" files. Local-only by design: each file captures
# the full conversation/decision history for one module so future AI runs can
# regenerate the module from scratch. Treat the folder as personal/shareable
# — copy or zip it out of the repo to share, never commit it.
Skills/

# Personal design / brainstorming notes — captured before an idea is mature
# enough to become an issue or a spec. Local-only.
Design-Notes/

# Jekyll local-preview output and lock file. The docs site is built by
# GitHub Pages from the repo source; nothing here belongs in git.
_site/
.jekyll-cache/
Gemfile.lock

# Local Ruby/Bundler install state (machine-specific)
.bundle/
vendor/bundle/

# OS/editor machine artifacts
.impeccable/
.idea/
.vs/
*.code-workspace
.DS_Store
Thumbs.db
Desktop.ini

# Logs / transcripts / temp artifacts from test and deployment runs
*.log
*.tmp
*.temp
*.bak
*.orig
*Transcript*.txt
*transcript*.txt

# Local environment and sensitive material (never commit to public repo)
.env
.env.*
*.pfx
*.p12
*.pem
*.key
*.snk
